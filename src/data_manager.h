#pragma once
/*
#include <string>
#include <unordered_map>
#include <vector>
#include <fstream>
#include "TrainingData.h"

//This is for saving training results and loading them to avoid re-training
//Not very useful unless debugging testing

//Not up to date with code

//saving to filename.bin
void save_training_data(const TrainingData& data, const std::string& filename);

//loading from filename.bin
TrainingData* load_training_data(const std::string& filename);

//checking if filename.bin exists and is ok
bool is_training_data_available(const std::string& filename);
*/